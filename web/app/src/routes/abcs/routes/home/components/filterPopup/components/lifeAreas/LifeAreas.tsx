export const LifeAreas = () => {
  return <></>;

  // const { t } = useTranslation();
  // const { updateAbcFilter } = useAbcFilterMutations();
  // const { items, loading } = useItems();
  // const { abcFilter } = useAbcFilter();
  //
  // if (loading) {
  //   return <LoadingIndicator />;
  // }
  //
  // const itemSelectorPressHandler = (item: Item) => {
  //   const lifeAreas = abcFilter?.lifeAreas || [];
  //
  //   const result = item.selected ? lifeAreas.filter(value => value !== item._id) : [...lifeAreas, item._id];
  //
  //   updateAbcFilter({ lifeAreas: result });
  // };
  //
  // return (
  //   <Container>
  //     <Text>{t('routes.abcs.lifeAreas')}</Text>
  //
  //     <ButtonBar items={items} onChange={itemSelectorPressHandler} />
  //   </Container>
  // );
};
